# Redirect all requests to /public
<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the request is for an existing file or directory, skip
    RewriteCond %{REQUEST_URI} !^/public/

    # Redirect everything else to /public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
